# Daily Notes - 2026-07-19

## Events & Discoveries

### Network Scan
Scanned the Bumble Cottage local network (`192.168.0.0/24`) and mapped Chris's self-hosted homelab environment.

### Active Services Map
- **Proxmox Virtual Environment (VE) Host:** `https://192.168.0.166:8006` (HP Hardware)
- **NexusTrader Algorithmic Trading Bot:** `http://192.168.0.144` / `http://192.168.0.144:8000` (Xen/Proxmox VM)
- **LaunchForge (Owntune / Life & Home Control Center):** `http://192.168.0.30` / `http://192.168.0.30:5000` (Local Container/VM)
- **Home Assistant instances:**
  - `http://192.168.0.193:8123` (ASUSTek Hardware)
  - `http://192.168.0.156:80` (Local Container/VM)
- **Bumble Cottage Core Homepage / Server:** `http://192.168.0.69`
- **Local Network Router / Gateway:** `http://192.168.0.1` (Tenda Hardware)

### OpenClaw provider config
- Configured Anthropic provider using `ANTHROPIC_API_KEY` env SecretRef and enabled the Anthropic plugin.
- Registered DeepSeek provider/catalog for `deepseek-v4-pro` and `deepseek-v4-flash`, but did not set an API SecretRef because `DEEPSEEK_API_KEY` was missing during validation.
- Chris explicitly asked Keith to provide progress updates during active work and avoid unnecessary paid/limited API calls.

## Notes for Next Steps
- Help Chris set up secure connections/routes to his work-sandboxed desktop and main comp repos.
- Configure SSH access to the Proxmox host and other nodes for Keith.
- Locate the Plesk server (not currently responding on standard ports `8443`/`8447` on the `.0.x` subnet).
- Add `DEEPSEEK_API_KEY` to the Gateway environment/secrets, then patch `models.providers.deepseek.apiKey` to `{ source: "env", provider: "default", id: "DEEPSEEK_API_KEY" }` and validate without doing a paid model call unless Chris asks.
- DeepSeek API key was configured in OpenClaw config and verified redacted in config output. No live DeepSeek model call was made.
- Live provider smoke tests requested by Chris: Anthropic request reached provider but failed with `authentication_error: API key is invalid`; DeepSeek request reached provider but failed with insufficient balance / no credits. No successful model response yet.
- Replaced stale Anthropic key in config and service env files; restarted Gateway; live Anthropic smoke test succeeded with `ANTHROPIC_OK` on `anthropic/claude-sonnet-4-6`.

### Token Optimization
- Trimmed AGENTS.md 81%, SOUL.md 41%, TOOLS.md 57% — total workspace files reduced from 11KB to 4KB (63%)
- Switched tools.profile from `full` to `coding` — removes ~10K+ tokens of unused tool schemas
- Switched default model from `ollama/llama3.2:latest` to `deepseek/deepseek-v4-flash`
- Set `thinkingDefault: low` and `fastModeDefault: true`
- Set `contextInjection: continuation-skip` — caches workspace context between continuation turns
- Session model pinned to `deepseek/deepseek-v4-flash`
- Estimated savings: ~13-21K input tokens per turn

### NexusTrader Hardening (full implementation)
Implemented entire audit patch plan:
- **evaluation/ package**: benchmark harness, metrics (Sharpe/Sortino/Calmar/PF/win rate/drawdown), cost model, calibration (Brier/ECE/Kelly)
- **evaluation/safety.py**: KillSwitch (daily loss, position, exposure, drawdown limits), DrawdownTracker, MutationFreeze
- **trading_modes.py**: DB namespace separation for research/paper/live modes
- **tests**: 51 unit tests for all new modules
- All 17 project source files compile clean on Python 3.8
- Wired KillSwitch check into main.py process_tick() — blocks trades when limits exceeded
- Wired PnL feeding into on_trade_closed() — routes trade outcomes to KillSwitch and DrawdownTracker
- Wired MutationFreeze into database.py save_setting() — agent-driven config changes are suggested, not applied
- Added KillSwitch/DrawdownTracker state save/restore in startup/shutdown events
- Added /api/safety/status endpoint for monitoring
- Added trading mode migration (existing settings → research: namespace) on startup
- All 10 source files compile clean (Python 3.8), 51/51 tests pass
